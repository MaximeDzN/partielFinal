spring:
  config:
    import: configserver:http://configuration:8888
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: 'true'
    hibernate:
      ddl-auto: create-drop
    show-sql: 'false'
    open-in-view: 'false'
    datasource:
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      url: jdbc:mysql://docker-mysql:3306/user?serverTimezone=UTC
    jmx:
      unique-names: 'true'
  application:
    name: user-service
  cloud:
    client:
      hostname: ${HOSTNAME}
    vault:
      uri: http://51.91.125.163:8200/
      connection-timeout: 5000
      read-timeout: 15000
      token: hvs.fQCDPLZuAGIfuxGFb2xbxSaU
      kv:
        enabled: true
        backend: secret
        profile-separator: '/'
        default-context: 'partielfinal'

eureka:
  instance:
    hostname: ${HOSTNAME}
  client:
    service-url:
      defaultZone: http://registration:8761

management:
  endpoints:
    web:
      exposure:
        include: '*'
    jmx:
      exposure:
        exclude: '*'

token:
  secret: hvs.fQCDPLZuAGIfuxGFb2xbxSaU

