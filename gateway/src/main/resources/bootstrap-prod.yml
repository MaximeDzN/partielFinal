#spring.application.name=service-gateway
#spring.config.import=configserver:http://configuration:8888
#eureka.instance.hostname=${HOSTNAME}
#spring.cloud.client.hostname=${HOSTNAME}
##SECRET
#spring.cloud.vault.uri=http://51.91.125.163:8200/
#spring.cloud.vault.connection-timeout= 5000
#spring.cloud.vault.read-timeout=15000
#spring.cloud.vault.token= hvs.fQCDPLZuAGIfuxGFb2xbxSaU
#spring.cloud.vault.kv.enabled=true
#spring.cloud.vault.kv.backend-backend=secret
#spring.cloud.vault.profile-separator ='/'
#spring.cloud.vault.default-context='partielfinal'
#token.secret=hvs.fQCDPLZuAGIfuxGFb2xbxSaU
spring:
#  config:
#    import: configserver:http://configuration:8888
  application:
    name: service-gateway
  cloud:
    gateway:
      discovery:
        enabled: 'true'
        locator:
          enabled: 'true'
          lowerCaseServiceId: 'true'
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: '*'
            allowedHeaders: '*'
            allowedMethods: '*'

    client:
      hostname: ${HOSTNAME}
    vault:
      uri: http://51.91.125.163:8200/
      connection-timeout: 5000
      read-timeout: 15000
      token: hvs.fQCDPLZuAGIfuxGFb2xbxSaU
      kv:
        enabled: true
        backend: secret
        profile-separator: '/'
        default-context: 'partielfinal'
eureka:
  client:
    service-url:
      defaultZone: http://registration:8761/eureka
  instance:
    hostname: ${HOSTNAME}


management:
  endpoints:
    web:
      exposure:
        include: '*'
      cors:
        allowed-origins: '*'
        allowed-methods: '*'
        allowed-headers: '*'
        allowed-origin-patterns: '*'
server:
  port: '8099'



